<h1>Locações</h1>

<%= link_to 'Registrar nova locação', new_rental_path %>

<% if @rentals.empty? %>
  Nenhuma locação cadastrada
<% end %>

<% @rentals.each do |ren| %>
  <h3><%= link_to ren.customer.name, rental_path(id: ren.id) %></h3>
  <!--<p><%= link_to 'Ver detalhes', ren, id: "details-#{ren.id}" %></p>-->
<% end %>
<!--
<% @rentals.each do |rental| %>
  <dl>
    <dt>Data de início:</dt>
    <dd><%= l rental.start_date %></dd>
    <dt>Data de término:</dt>
    <dd><%= l rental.end_date %></dd>
    <dt>Cliente:</dt>
    <dd><%= rental.customer.name %></dd>
    <dt>Categoria:</dt>
    <dd><%= rental.car_category.name %></dd>
  </dl>
<% end %>-->

<%= link_to 'Voltar', root_path %>

