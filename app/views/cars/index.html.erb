<h1>Carros da frota</h1>

<%= link_to 'Registrar novo carro', new_car_path %>

<% if @cars.empty? %>
  Nenhum carro cadastrado
<% end %>

<% @cars.each do |c| %>
  <h3><%= link_to c.car_model.name, car_path(id: c.id) %></h3>
<% end %>

<%= link_to 'Voltar', root_path %>